import { createClient } from "https://esm.sh/@supabase/supabase-js@2";
import WhatsAppAgent from "https://raw.githubusercontent.com/maliq273/isaacs-and-partners-website/49b9c5a88136b63ed89578145f8a96e3c846c323/app/communication/agents/WhatsAppAgent.js";
import AIProviderService from "https://raw.githubusercontent.com/maliq273/isaacs-and-partners-website/682e60be1a1ecd36842c8771aea695fad70b6562/app/ai/providers/AIProviderService.js";
import CompanyTruthService from "https://raw.githubusercontent.com/maliq273/isaacs-and-partners-website/617e9509f3e62df736682e9d75fb87574e3949fe/app/ai/CompanyTruthService.js";
import TruthFusionEngine from "https://raw.githubusercontent.com/maliq273/isaacs-and-partners-website/682e60be1a1ecd36842c8771aea695fad70b6562/app/ai/TruthFusionEngine.js";
import HistoricalMemoryRetrievalService from "https://raw.githubusercontent.com/maliq273/isaacs-and-partners-website/efcc2931c429a820666e4ef287cbc98d12c60724/app/ai/HistoricalMemoryRetrievalService.js";
import CustomerRelationshipMemoryEngine from "https://raw.githubusercontent.com/maliq273/isaacs-and-partners-website/ab698e4002b0d66ae8b6157d2e13978f67e4040f/app/ai/CustomerRelationshipMemoryEngine.js";

const SUPABASE_URL=Deno.env.get("SUPABASE_URL");const SERVICE_ROLE_KEY=Deno.env.get("SUPABASE_SERVICE_ROLE_KEY");const ANON_KEY=Deno.env.get("SUPABASE_ANON_KEY")??Deno.env.get("SUPABASE_PUBLISHABLE_KEY");const INTERNAL_WORKER_TOKEN=Deno.env.get("OPENWA_WORKER_TOKEN");const ALLOWED_ORIGIN=Deno.env.get("AI_LIAISON_ALLOWED_ORIGIN")??"https://www.isaacsandpartners.online";if(!SUPABASE_URL||!SERVICE_ROLE_KEY)throw new Error("AI liaison runtime configuration is incomplete.");const admin=createClient(SUPABASE_URL,SERVICE_ROLE_KEY,{auth:{autoRefreshToken:false,persistSession:false}});const corsHeaders={"Access-Control-Allow-Origin":ALLOWED_ORIGIN,"Access-Control-Allow-Headers":"authorization, x-client-info, apikey, content-type, x-ai-internal-worker-token","Access-Control-Allow-Methods":"POST, OPTIONS","Content-Type":"application/json"};
function json(b:any,s=200){return new Response(JSON.stringify(b),{status:s,headers:corsHeaders})}function clean(v:any,m=4096){return String(v??"").trim().slice(0,m)}function nullable(v:any,m=255){const t=clean(v,m);return t||null}function channelOf(v:any){const c=clean(v,32).toUpperCase()||"PORTAL";if(!["PORTAL","WHATSAPP","EMAIL","OTHER"].includes(c))throw new Error("Invalid conversation channel.");return c}function isDirectChat(id:string){const c=clean(id,255).toLowerCase();return !!c&&!c.endsWith("@g.us")&&!c.endsWith("@broadcast")&&!c.endsWith("@newsletter")&&c!=="status@broadcast"&&(c.endsWith("@c.us")||c.endsWith("@lid")||/^\d{6,20}$/.test(c))}
async function authenticate(req:Request,p:any){const it=clean(req.headers.get("X-AI-Internal-Worker-Token"),512);if(it){if(!INTERNAL_WORKER_TOKEN||it!==INTERNAL_WORKER_TOKEN)throw new Response(JSON.stringify({error:"Invalid internal worker credentials."}),{status:401,headers:corsHeaders});const uid=clean(p?.userId,128);if(!uid)return{caller:admin,user:null,internal:true};const{data,error}=await admin.auth.admin.getUserById(uid);if(error||!data?.user?.id)throw new Response(JSON.stringify({error:"Internal client could not be resolved."}),{status:404,headers:corsHeaders});return{caller:admin,user:data.user,internal:true}}if(!ANON_KEY)throw new Response(JSON.stringify({error:"AI portal authentication is not configured."}),{status:503,headers:corsHeaders});const token=clean(req.headers.get("Authorization")).replace(/^Bearer\s+/i,"");if(!token)throw new Response(JSON.stringify({error:"Authentication is required."}),{status:401,headers:corsHeaders});const caller=createClient(SUPABASE_URL,ANON_KEY,{global:{headers:{Authorization:`Bearer ${token}`}},auth:{autoRefreshToken:false,persistSession:false}});const{data,error}=await caller.auth.getUser(token);if(error||!data?.user?.id)throw new Response(JSON.stringify({error:"Authenticated user could not be verified."}),{status:401,headers:corsHeaders});return{caller,user:data.user,internal:false}}
async function matterFor(uid:string|null,mid:string|null){if(!uid||!mid)return null;const{data,error}=await admin.from("matters").select("*").eq("id",mid).maybeSingle();if(error)throw error;if(!data)return null;if(data.individual_user_id===uid)return data;if(data.business_id){const r=await admin.from("businesses").select("id").eq("id",data.business_id).eq("owner_user_id",uid).maybeSingle();if(r.error)throw r.error;if(r.data)return data}throw new Error("You are not authorised to use this matter in the AI conversation.")}
async function operational(uid:string|null,mid:string|null){const out:any={matter:null,userMatters:[],documents:[],appointments:[],invoices:[]};if(!uid)return out;if(mid)out.matter=await matterFor(uid,mid);const bs=await admin.from("businesses").select("id").eq("owner_user_id",uid);const ids=(bs.data||[]).map((b:any)=>b.id);let q:any=admin.from("matters").select("id,matter_number,reference,status,service_type,service_domain,department,title,description,priority,workflow_status,created_at,updated_at,due_date").order("updated_at",{ascending:false});q=ids.length?q.or(`individual_user_id.eq.${uid},business_id.in.(${ids.join(",")})`):q.eq("individual_user_id",uid);const mr=await q;if(!mr.error)out.userMatters=mr.data||[];const run=async(t:string,s:string,o:string,owner:string,n=50)=>{let x:any=admin.from(t).select(s).order(o,{ascending:false}).limit(n);x=mid?x.eq("matter_id",mid):x.eq(owner,uid);const r=await x;if(r.error)throw r.error;return r.data||[]};out.documents=await run("documents","id,matter_id,document_type,name,status,required,reviewed,uploaded_at,reviewed_at,notes,created_at,updated_at","updated_at","individual_user_id");out.appointments=await run("appointments","id,matter_id,title,appointment_type,starts_at,ends_at,status,location,notes","starts_at","individual_user_id",20);out.invoices=await run("invoices","id,matter_id,invoice_number,description,amount,currency,status,due_at,issued_at,paid_at","created_at","individual_user_id",20);return out}
async function conversation(uid:string|null,chatId:string,phone:string|null,channel:string,mid:string|null){const q=await admin.from("ai_conversations").select("*").eq("channel",channel).eq("chat_id",chatId).order("updated_at",{ascending:false}).limit(1).maybeSingle();if(q.error)throw q.error;if(q.data){if(q.data.client_user_id&&uid&&q.data.client_user_id!==uid)throw new Error("Conversation belongs to another user.");return q.data}const r=await admin.from("ai_conversations").insert({client_user_id:uid,matter_id:mid,chat_id:chatId,phone_number:phone,channel,state:"AI_ACTIVE",metadata:{source:"ai-liaison-runtime",created_by:"trusted-runtime"}}).select("*").single();if(r.error)throw r.error;return r.data}
async function history(cid:string){const r=await admin.from("ai_conversation_messages").select("id,direction,body,sender_type,metadata,created_at,intent,service_domain").eq("conversation_id",cid).order("created_at",{ascending:false}).limit(40);if(r.error)throw r.error;return(r.data||[]).reverse().map((x:any)=>({id:x.id,direction:x.direction,body:x.body,sender:x.sender_type,messageId:x.id,metadata:x.metadata||{},createdAt:x.created_at,intent:x.intent||null,serviceDomain:x.service_domain||null}))}
async function append(cid:string,sender:string,dir:string,body:string,intent:any,domain:any,metadata:any){const r=await admin.rpc("ai_append_conversation_message",{p_conversation_id:cid,p_sender_type:sender,p_direction:dir,p_body:body,p_intent:intent,p_service_domain:domain,p_metadata:metadata||{}});if(r.error)throw r.error;return r.data}
async function persistMemories(records:any[]){for(const record of records||[]){let q:any=admin.from("ai_customer_memories").select("id").eq("category",record.category).eq("memory_key",record.memory_key).eq("value_text",record.value_text).limit(1);if(record.contact_id)q=q.eq("contact_id",record.contact_id);else if(record.client_user_id)q=q.eq("client_user_id",record.client_user_id);else if(record.conversation_id)q=q.eq("conversation_id",record.conversation_id);const existing=await q.maybeSingle();if(existing.error&&existing.error.code!=="PGRST116")throw existing.error;if(existing.data?.id){const u=await admin.from("ai_customer_memories").update({last_confirmed_at:record.last_confirmed_at,last_seen_at:record.last_seen_at,status:"ACTIVE",evidence:record.evidence,source_message_id:record.source_message_id,metadata:record.metadata}).eq("id",existing.data.id);if(u.error)throw u.error}else{const ins=await admin.from("ai_customer_memories").insert(record);if(ins.error)throw ins.error}}}
async function queue(con:any,uid:string|null,body:string,phone:string|null,mid:string|null,source:string|null){if(!isDirectChat(con.chat_id))throw new Error("Outbound WhatsApp delivery is restricted to direct contacts.");const key=`ai-reply:${con.id}:${source||Date.now()}`;const ex=await admin.from("communication_messages").select("id").eq("idempotency_key",key).maybeSingle();if(ex.error&&ex.error.code!=="PGRST116")throw ex.error;if(ex.data?.id)return ex.data.id;const m=await admin.from("communication_messages").insert({customer_user_id:uid,matter_id:mid||con.matter_id||null,channel:"WHATSAPP",direction:"OUTBOUND",phone_number:phone||con.phone_number||null,chat_id:con.chat_id,body:body.slice(0,4096),status:"QUEUED",idempotency_key:key,metadata:{source:"ai-liaison-runtime",conversation_id:con.id,source_message_id:source}}).select("id").single();if(m.error)throw m.error;const o=await admin.from("communication_outbox").insert({message_id:m.data.id,session_id:null,chat_id:con.chat_id,status:"QUEUED"});if(o.error)throw o.error;return m.data.id}
function agent(){const p=new AIProviderService();const c=new CompanyTruthService();return new WhatsAppAgent({responseGenerator:(input:any)=>new TruthFusionEngine({provider:p,companyTruth:c}).generate(input)})}
const historicalRetriever=new HistoricalMemoryRetrievalService();const relationshipMemory=new CustomerRelationshipMemoryEngine();
Deno.serve(async req=>{if(req.method==="OPTIONS")return new Response("ok",{status:200,headers:corsHeaders});if(req.method!=="POST")return json({error:"Method not allowed."},405);try{const p=await req.json();const{caller,user,internal}=await authenticate(req,p);const body=clean(p?.body);if(!body)return json({error:"Message body is required."},400);const channel=channelOf(p?.channel);if(internal&&channel!=="WHATSAPP")return json({error:"Internal worker requests are restricted to WhatsApp."},403);if(!internal&&channel!=="PORTAL")return json({error:"Browser AI requests are restricted to the client portal."},403);const chatId=nullable(p?.chatId,255)||(user?`portal:${user.id}`:"portal:unknown");const phone=nullable(p?.phoneNumber,64);const mid=nullable(p?.matterId,64);const msgId=nullable(p?.messageId,255);if(channel==="WHATSAPP"&&(!p?.chatId||!isDirectChat(chatId)))return json({error:"WhatsApp AI is restricted to direct contacts, not groups or broadcasts."},403);if(!internal&&chatId!==`portal:${user.id}`)return json({error:"Portal chat identity is server-controlled."},403);if(!internal){const a=await caller.rpc("client_portal_access_status");if(a.error)throw a.error;if(String(Array.isArray(a.data)?a.data[0]:a.data).toUpperCase()!=="APPROVED")return json({error:"Client portal access is not approved."},403)}let contact=null;if(channel==="WHATSAPP"){if(p?.contactId){const x=await admin.from("communication_contacts").select("*").eq("id",p.contactId).maybeSingle();if(x.error)throw x.error;contact=x.data}if(!contact){const x=await admin.from("communication_contacts").select("*").eq("chat_id",chatId).maybeSingle();if(x.error)throw x.error;contact=x.data}}const uid=user?.id||contact?.user_id||null;const matter=await matterFor(uid,mid);const op=await operational(uid,mid);const con=await conversation(uid,chatId,phone,channel,mid);const prior=await history(con.id);const historicalMemory=await historicalRetriever.retrieve({db:admin,conversationId:con.id,contactId:contact?.id||null,userId:uid,query:body,history:prior});const clientMsg=await append(con.id,"CLIENT","INBOUND",body,nullable(p?.intent,100),nullable(p?.serviceDomain,100),{source:"ai-liaison-runtime",transport:internal?"openwa":"portal",...(msgId?{client_message_id:msgId}:{})});if(["HUMAN_ACTIVE","AI_ESCALATED"].includes(String(con.state||"").toUpperCase()))return json({ok:true,conversation:con,message:clientMsg,aiMessage:null,result:{action:"ROUTE_TO_HUMAN",escalated:true},context:{historyLoaded:prior.length,maxHistory:40,historicalMemoryFound:historicalMemory.found}});const onboardingStates=new Set(["NEW","ASK_WHATSAPP_CONSENT","ASK_MATTER","ASK_EMAIL","ASK_NAME","ASK_ACCOUNT_TYPE","IDENTITY_MATCHING"]);const shouldOnboard=channel==="WHATSAPP"&&contact&&!contact.user_id&&onboardingStates.has(String(contact.onboarding_state||"NEW").toUpperCase());if(shouldOnboard){const ctx={id:con.id,chatId,phoneNumber:contact.phone_number,state:"AI_ACTIVE",facts:contact.onboarding_facts||con.facts||{},memory:con.facts?.customerMemory||{},messages:prior};const r=await agent().handleInbound({chatId,phoneNumber:contact.phone_number,body,messageId:msgId,user:null,matter:null,operationalContext:op,conversation:ctx,contact,historicalMemory});if(r?.onboarding){const facts=r.facts||{};const u:any={onboarding_state:r.nextState,onboarding_facts:facts,updated_at:new Date().toISOString()};if(facts.whatsappConsent!==undefined)u.whatsapp_consent=Boolean(facts.whatsappConsent);if(facts.preferredChannel!==undefined)u.preferred_channel=facts.preferredChannel||null;if(facts.email)u.email=clean(facts.email,320).toLowerCase();if(facts.firstName)u.first_name=clean(facts.firstName,120);if(facts.lastName)u.last_name=clean(facts.lastName,120);if(facts.claimedAccountType)u.claimed_account_type=String(facts.claimedAccountType).toUpperCase();const ur=await admin.from("communication_contacts").update(u).eq("id",contact.id).select("*").single();if(ur.error)throw ur.error;const records=relationshipMemory.observe({body,contact:ur.data,user:null,onboardingFacts:facts,conversationId:con.id,sourceMessageId:clientMsg?.id||null}).records;await persistMemories(records);const reply=clean(r.reply,8192);const aiMsg=reply?await append(con.id,"AI","OUTBOUND",reply,null,null,{source:"ai-liaison-runtime",onboarding:true,onboarding_state:ur.data.onboarding_state}):null;const out=reply?await queue(con,ur.data.user_id||null,reply,ur.data.phone_number,null,msgId):null;await admin.from("ai_conversations").update({facts:{...(con.facts||{}),...facts},updated_at:new Date().toISOString()}).eq("id",con.id);return json({ok:true,onboarding:true,contact:ur.data,conversation:con,message:clientMsg,aiMessage:aiMsg,transportMessageId:out,context:{historyLoaded:prior.length,maxHistory:40,historicalMemoryFound:historicalMemory.found}})}}const relationshipRecords=relationshipMemory.observe({body,contact,onboardingFacts:contact?.onboarding_facts||{},user,conversationId:con.id,sourceMessageId:clientMsg?.id||null}).records;await persistMemories(relationshipRecords);const ctx={id:con.id,chatId,phoneNumber:con.phone_number||phone,state:con.state||"AI_ACTIVE",facts:con.facts||{},memory:con.facts?.customerMemory||{},lastIntent:con.last_intent||null,lastService:con.last_service||null,messages:prior};const result=await agent().handleInbound({chatId,phoneNumber:ctx.phoneNumber,body,messageId:msgId,user,matter,operationalContext:op,conversation:ctx,contact,historicalMemory});const reply=clean(result?.reply,8192);const aiMsg=reply?await append(con.id,"AI","OUTBOUND",reply,result?.intent?.intent||null,result?.servicePlan?.domain||null,{source:"ai-liaison-runtime",runtime:"WhatsAppAgent",action:result?.action||"RESPOND",ai_provider:result?.aiProvider||null,ai_model:result?.aiModel||null,company_truth_sources:result?.companySources||[],context_messages_loaded:prior.length,historical_memory_found:historicalMemory.found}):null;const facts={...(con.facts||{}),...(result?.context?.facts||{}),customerMemory:result?.context?.memory||con.facts?.customerMemory||{}};await admin.from("ai_conversations").update({facts,last_intent:result?.intent?.intent||con.last_intent||null,last_service:result?.servicePlan?.service?.name||con.last_service||null,updated_at:new Date().toISOString()}).eq("id",con.id);let out=null;if(reply&&channel==="WHATSAPP"&&result?.action==="RESPOND")out=await queue(con,uid,reply,phone,mid,msgId);return json({ok:true,conversation:con,message:clientMsg,aiMessage:aiMsg,transportMessageId:out,result:{action:result?.action||null,intent:result?.intent||null,servicePlan:result?.servicePlan||null,sales:result?.sales||null,escalated:result?.action==="ESCALATE",aiProvider:result?.aiProvider||null,aiModel:result?.aiModel||null,companySources:result?.companySources||[]},context:{historyLoaded:prior.length,maxHistory:40,historicalMemoryFound:historicalMemory.found,historicalRecall:historicalMemory.isHistoricalRecall}})}catch(e){if(e instanceof Response)return e;console.error("AI liaison runtime failed",e);return json({error:e instanceof Error?e.message:"AI liaison runtime failed."},500)}});
