project_id = "aglobzjtstbfwcsdhvmp"

[functions.admin-github-config]
verify_jwt = false

[functions.admin-upload-brand-asset]
verify_jwt = false

# PR40 — invoked only by the trusted worker boundary. The function itself
# validates the service-role bearer token before processing any queue job.
[functions.trusted-document-worker]
verify_jwt = false

# PR43 — OpenWA webhook and outbound worker. Authentication is performed
# inside the function using HMAC for webhooks and a dedicated worker token
# for outbound queue processing.
[functions.openwa-communication-worker]
verify_jwt = false
