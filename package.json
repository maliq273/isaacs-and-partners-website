{
  "name": "isaacs-and-partners-website",
  "version": "1.0.0",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "node server.js",
    "start": "node server.js",
    "build": "echo 'No build step required'",
    "test": "node scripts/test.mjs",
    "lint": "node scripts/lint.mjs",
    "production-check": "node scripts/production-check.mjs --write",
    "production-check:ci": "node scripts/production-check.mjs --ci --write"
  },
  "dependencies": {
    "@google/genai": "^2.23.0",
    "express": "^4.19.2"
  }
}
